"""Infrastructure layer (L2) for the orders context.

Django ORM repositories, the Redis event publisher and other adapters will live
here in later Stories. Empty skeleton for now so the bounded context exposes all
four layers (NSG-16); no Django model is introduced yet (out of scope).
"""
