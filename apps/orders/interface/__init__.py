"""Interface layer (L3) for the orders context.

DRF views, serializers and URL routing for ``/api/v1/orders/`` will live here in
later Stories. Empty skeleton for now so the bounded context exposes all four
layers (NSG-16).
"""
