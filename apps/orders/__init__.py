"""The `orders` bounded context.

Owns manufacturing orders (OFs): their lifecycle, invariants and — in later
Stories — their persistence, REST surface and domain events. See
ARCHITECTURE.md for the layer rules this package is organised around.
"""
