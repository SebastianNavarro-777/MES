"""Application layer (L1) for the orders context.

Use cases (e.g. ``create_order``, ``release_order``) and the Protocols they
depend on will live here in later Stories. Empty skeleton for now so the
bounded context exposes all four layers (NSG-16).
"""
