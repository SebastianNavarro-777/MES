"""Domain layer (L0) for the wip bounded context.

Pure Python + stdlib only (GP-001): entities, value objects and exceptions.
No Django, DRF or any third-party package may be imported from here.
"""
