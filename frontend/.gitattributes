# pnpm-lock.yaml is generated by `pnpm install`. Mark it generated so a
# ~3k-line lockfile cannot bury real changes in a PR diff/review. Content is
# unchanged; CI still consumes it via `pnpm install --frozen-lockfile`.
pnpm-lock.yaml -diff linguist-generated=true
